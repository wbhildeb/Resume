# Ignore everything
*

# Except for
!.gitignore
!*.tex
!*.cls
!Makefile

# PDF handling: only allow public (redacted) version
!resume-public.pdf
!contact-info.tex.template

# But ignore contact info (contains private email/phone)
contact-info.tex
contact-info.tex.backup
